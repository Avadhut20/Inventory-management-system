# React + TypeScript + Vite
## Step 1: Install React app using react vite

Use following command to install react:
``` npm install vite ```
Dwonload the packages using following command:
 ``` npm install```


